import { Grid, styled } from "@mui/material";

export const OptionsWrapper = styled(Grid)(() => ({
	display: "flex",
	padding: "2rem",
	backgroundColor: "white",
	width: "50%",
	margin: "3rem",
	borderRadius: "1rem",
}));
